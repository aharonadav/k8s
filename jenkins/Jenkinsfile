pipeline {
    agent {
     kubernetes {
      //cloud 'kubernetes'
      yamlFile 'KubernetesPod.yaml'
    }
  }
  stages {
      stage('Run docker') {
          steps {
              container('docker') {
                  sh 'docker --version'
              }
          }
      }
  }
}