apiVersion: v1
kind: Pod
metadata:
  labels:
    some-label: random-value
spec:
  containers:
  - name: docker
    image: docker
    command:
      - cat
    tty: true
    privileged: true
    volumeMounts:
      - name: dockersock
        mountPath: /var/run/docker.sock
  - name: awscli
    image: aws-jnlp
    command:
      - cat
    tty: true
    privileged: true
  volumes:
  - name: dockersock
    hostPath:
      path: /var/run/docker.sock
    